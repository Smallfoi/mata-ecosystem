# MODULES — карта модулей экосистемы МАТА

Экосистема разбита на **модули** — самостоятельные куски функциональности. Это
помогает Claude и Codex (и владельцу) сразу понимать: что за модуль трогаем, где он
живёт (какое приложение + бэкенд) и в каком он статусе.

**Правило:** меняешь модуль — обнови его **Статус** здесь и запись в `docs/WORKLOG.md`.
Контракт API по модулям — в `ECOSYSTEM_API.md`; решения — в `docs/DECISIONS.md`.

**Легенда статусов:** ✅ готово · 🟡 частично / локально (ждёт бэк) · 🔴 план (ещё нет)

---

## 1. Ядро (общее для всех приложений, источник правды — backend)

| Модуль | Что входит | Где живёт | Статус |
|---|---|---|---|
| **Account & Auth** (аккаунт и авторизация) | SSO по телефону/email, JWT, единый `user_id` на все приложения | backend + Квартал + Store + Сайт | ✅ |
| **Profile** (профиль) | имя, телефон, город; **единый аватар + email + структурный адрес** (город/улица/дом/корпус/кв) — централизованы: меняешь где угодно (приложение/сайт) → меняется везде; бэк — источник правды; **права субъекта ПДн (§2): выгрузка `GET /account/export` + исправление `PATCH /profile` + удаление `POST /account/delete`** | backend + Квартал + Store + Сайт | ✅ |
| **Loyalty / Points** (баллы) | баланс, уровни, транзакции, история; начисление (Квартал) и списание (Store) | backend + Квартал + Store | ✅ |
| **Notifications** (уведомления) | лента событий: статусы заказов (авто), заявки/одобрения в клуб, новый уровень лояльности (авто) | backend ✅ + **чтение в Store и Квартале** ✅ (лента/бейдж/пометка прочитанным); FCM-пуш — план (нужен провайдер) | 🟡 |

## 2. Квартал (Runner)

| Модуль | Что входит | Где живёт | Статус |
|---|---|---|---|
| **Run / GPS** (пробежки) | трекинг, маршрут, сглаживание, история, метрики | Квартал (локально) + **синк сводок на бэк** (`runs`, идемпотентно): **push** при завершении + **pull** кросс-девайс/после переустановки (route-less карточки); сырой GPS — локально (приватность §2); **серверный расчёт очков за бег + валидация** (анти-чит S-04 Phase 1, D-23: скорость/дистанция/суточный лимит → flagged); **разбор помеченных человеком** (S-04 ф.2, D-66: страница «Проверка забегов», решения approve/reject/пересчёт, мульти-аккаунт по общему устройству, уведомление бегуну) | ✅ (push+pull + серверные очки + модерация; territory/purchase-очки на сервер — остаток S-04) |
| **Territories** (территории) | захват по реальному маршруту (D-09), живой слой 7д + вечный след (D-14), античит | backend ✅ (PostGIS: capture/list/footprint; **очки за захват +50 серверно**, S-04 D-23) + Квартал UI; **плановая чистка ✅** (Celery beat ежедневно: протухшие зоны/защиты + старая идемпотентность `processed_captures` >30д — таблицы не пухнут) | ✅ |
| **Map** (карта) | тайлы (OSM), слои территорий/позиции, зум/LOD | Квартал; прод тайл-провайдер — позже | ✅ |
| **Clubs** (клубы) | создание, участники, заявки, роли, агрегаты (D-10), вступление по коду/ссылке/**QR-скану**; **стиль/лого/обложка клуба, челленджи, территория, рейтинг** | backend ✅ / UI в Квартале (клуб доведён до 100%) | ✅ |
| **Leaderboard** (рейтинг) | вкладки Личный / Клубы / Районы (по км, D-11) | backend ✅ + UI; «Районы» по реальному контролю зон — план | 🟡 |
| **Achievements** (достижения) | бейджи по активности (пробежки/км/захваты/уровень) | Квартал (локальный расчёт) | ✅ базово |
| **Challenges** (вызовы) | **клубные челленджи** (цель недели по км, создание/отмена владельцем) | backend ✅ + Квартал UI | ✅ клубные (личные — позже) |
| **Tools** (инструменты бегуна) | офлайн-калькуляторы и таймеры: темп↔скорость, пульсовые зоны (Z1–Z5), размер кроссовок, метроном каденса, интервальный таймер (вход: Профиль → «Инструменты») | Квартал (локально, без бэка) | ✅ |

## 2б. Квартал 2.0 — новая геймификация (бывш. «Лига», D-59 → D-70)

Строилась с 29.08.2026 в отдельном dev-приложении «ЛИГА»; 05.09.2026 эта кодовая
база стала боевым Кварталом (`mata_kvartal/`, D-70). План работ — `docs/LEAGUE_PLAN.md`.

| Модуль | Что входит | Где живёт | Статус |
|---|---|---|---|
| **Каркас приложения** | карта, забеги, клубы, баллы, профиль | Квартал | ✅ |
| **Профиль бегуна** | год рождения, пол, уровень, цель на неделю — основа «своей лиги» | backend + Квартал | 🔴 (Э1 плана) |
| **Зачёты** | абсолютный, личный рекорд, постоянство, своя лига, клубный; «ты обошёл N из M» | backend + Квартал | 🔴 (Э1 плана) |
| **Импорт тренировок** | общий приём `POST /workouts/import` + Health Connect (Android) в приложении; идемпотентность, склейка со своим забегом, античит | backend + Квартал | 🟡 (Health Connect ✅; файлы, Apple Health, партнёрские API — впереди, `docs/WEARABLES_PARTNERS.md`) |
| **Тропы** | отрезки, попытки, четыре доски, античит; трек живёт 14 дней и удаляется (D-60) | backend + Квартал | ✅ (автоматическое рождение троп — позже) |
| **Режимы и онбординг** | «зачем ты бегаешь» → стартовый экран под ответ | Квартал | 🟡 (вопрос есть; мирный режим территорий — впереди) |
| **Награды «Штамп МАТА»** | 44 медали (дизайн D-64, `docs/design/medals/`); backend `medals` (ленивая выдача + гравировка, `GET /v1/me/medals`), Зал славы + каталог «Все медали», чеканка, 3D-реверс, живые эмблемы у всех 44 (послойный конвейер, 05.09) | backend + Квартал | 🟡 (30 из 44 судятся сервером; 14 «спящих» критериев ждут решений владельца) |

## 3. Store (магазин)

| Модуль | Что входит | Где живёт | Статус |
|---|---|---|---|
| **Catalog** (каталог) | товары, категории, фильтры, поиск, баннеры | backend ✅ + Store (release) | ✅ |
| **Cart & Orders** (корзина и заказы) | checkout, доставка, история заказов (кросс-девайс), трата/начисление баллов | backend ✅ + Store; **начисление за покупку серверно** (purchase+registration в /orders, S-04 D-23); реальная оплата — план | ✅ |
| **Shoes / ShoeAsset** (кроссовки) | покупка → авто-ресурс износа; Квартал показывает трекер и убавляет км пробежками | backend ✅ + Квартал UI ✅ (карточка профиля + экран «Мои кроссовки» + начисление км после пробежки, идемпотентно по runId) | ✅ |
| **Reviews** (отзывы) | отзывы на товары (только купившие), рейтинг/пересчёт, **фото-вложения** (до 5), модерация (скрытие, не влияет на рейтинг) | backend ✅ + Store + Сайт | ✅ |
| **Обмен с 1С** | четыре потока: категории, карточки, цены и остатки (1С → МАТА) + заказы с подтверждением приёма и возвратом статуса (МАТА → 1С). Право вето владельца на 6 полей, остаток по размерам до витрины, журнал обмена, пакетная запись. Паспорт для 1С-ника: `docs/INTEGRATION_1C.md` | backend ✅ (прод) | ✅ (этап 1 закрыт; склады/штрихкоды/акции — этап 2) |

## 4. Сайт МАТА

| Модуль | Что входит | Где живёт | Статус |
|---|---|---|---|
| **Site / Content** (сайт, контент) | бренд-сайт, витрина; вход в общий аккаунт + общие баллы; **профиль: аватар (смена), email, структурный адрес; отзывы с фото** — всё централизовано с приложениями; **контент сайта редактируется через Конструктор** (тексты/фото шапки, hero, секций, футера — мини-CMS `SiteContent` + `content.js`, дефолты в HTML) | backend-интеграция ✅ (`ecosystem.js`/`catalog.js`/`content.js`); контент правится в админке-конструкторе, красота — за владельцем | 🟡 |

## 5. Платформа / инфраструктура

| Модуль | Что входит | Где живёт | Статус |
|---|---|---|---|
| **Admin panel** (админка) | Django `/admin/` (unfold): товары/каталог/баннеры (загрузка фото — товары/**баннеры/категории**), заказы (**bulk-статусы**), клубы + **челленджи**, баллы (реестр read-only), пользователи, **отзови/забеги в меню**; **модерация (S-10): бан аккаунта + скрытие клуба + скрытие/показ отзывов** (пересчёт рейтинга) + одобрить/отклонить флаг-забег; **экспорт CSV** (заказы/юзеры/баллы); **Конструктор витрины = «2-в-1» превью+редактор** (`/admin/merch/`): грузит РЕАЛЬНЫЙ сайт (`:5577`) и приложение (web-сборка, в рамке телефона, `:5578`) в iframe с `?edit=1` — **точная копия прода**, вкладки Сайт/Приложение. Правка кликом: **любой текст/кнопка и фото сайта** (мини-CMS `SiteContent`, `[data-edit]`/`[data-edit-img]`, `GET /v1/site/content` — hero-кнопки, фичи, режимы, фильтры, вся «Экосистема», референсы, футер), **перетаскивание ЛЮБЫХ групп блоков** (`[data-sortable]`/`[data-sid]` → ключ `order.<group>`: hero-кнопки, cinema, фичи, режимы, фильтры каталога, шаги лояльности, референсы), **скрытие/возврат блоков** (`[data-hideable]` → `hidden.<key>`), **добавление/удаление блоков** (репитеры `<template>` → `extra.<group>`), **выравнивание групп** (`align.<key>`) и **анимация блоков** (`anim.<key>`, 7 пресетов), **порядок товаров РАЗДЕЛЬНО по площадкам** (drag; `sort_site`/`sort_app`, API `?platform=`), **поля товара** (цена/описание/наличие/публикация — данные общие). Всё копится в **черновик** → кнопка «Опубликовать на сайт» с подтверждением. Мост кросс-домен через `postMessage` (`editor.js` на сайте, `console_bridge.dart` в приложении). **Баннеры (промо) — CRUD прямо в конструкторе** (кнопка «🖼 Баннеры»: создать/править/фото/порядок ↑↓/удалить; staff-эндпоинты `/admin/merch/banner*`), плюс остаются в `BannerAdmin` (fallback). *(future: инлайн-баннеры в промо-стрипе превью, тексты приложения)*; дашборд эко-метрик (устойчив к сбою PostGIS); **массовая рассылка уведомлений** (админ-действие `send_notification` на пользователях — промежуточная форма заголовок/текст → уведомление каждому) | backend ✅ (Django admin) | ✅ (аудит A/B пройден; роль «Модератор» ✅, массовая рассылка ✅; N+1-оптимизация списков — осталось) |
| **Insights / Analytics** (аналитика) | статистика пользователю и админу: активность, продажи, удержание | backend + admin: дашборд с эко-метриками; **тяжёлые агрегаты в Redis-кэше** (D-29); **событийная аналитика** (D-30: app `analytics`, поток `Event` — регистрация/забег/покупка/захват серверно + `POST /v1/events` клиентские; **ретеншн по когортам** `analytics_retention`; **воронка активации + активные + топ событий** `analytics_funnel`, D-30 фаза 2) | 🟡 (админ-дашборд ✅; кэш агрегатов ✅; событийная аналитика + когортный ретеншн + воронки ✅ backend; виджеты аналитики в админке + пользовательская — позже) |
| **Infrastructure** (инфра) | Django+DRF, Postgres/PostGIS + **Redis + Celery (worker/beat)** в Docker, CI, прод-конфиг (env/флаги), **медиа-хранилище** | вся экосистема | 🟡 (Django/Postgres/Docker/CI/прод-конфиг ✅; **Redis/Celery ✅** D-07; **медиа S3/Object Storage ✅** D-31 — аватары/фото/баннеры/лого в S3 при `MEDIA_S3_*`, иначе локальный диск; graceful degradation по env; **авто-удаление данных по срокам хранения ✅** (152-ФЗ §2: beat `cleanup_old_data` — старые события/прочитанные уведомления); реальный хостинг/бакет — за владельцем) |
| **Launch readiness** (запуск/право/приватность) | production-облако, согласия, политика ПД, GPS-приватность, оферта Store, правила баллов/контента, бэкапы и go/no-go | вся экосистема; `docs/LAUNCH_READINESS.md` | 🔴 (требования зафиксированы; реализация и юридическая проверка впереди) |

---

## Принцип «что строим на бэке»
Бэкенд — для данных, которые должны быть **общими и настоящими** (один аккаунт, общие
баллы, клубы, скоро — пробежки и территории). Чисто визуальное/локальное (навигация,
анимации, настройки отображения) бэкенд не требует. Приложение = интерфейс + кэш,
«правда» — на сервере. См. [[project-ecosystem-context]] и `docs/DECISIONS.md`.
